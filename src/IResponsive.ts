export interface IDeviceInfo {
    deviceType: string;
    deviceTypeVariant: string;
    orientation: string;
    width: number;
    height: number;
    isFallback: boolean;
}

export interface IWindowDimension {
    width: number;
    height: number;
}